@{
    // Breadcrumb
    ViewBag.Title = "Dashboard";

    ViewBag.Breadcrumb = new Breadcrumb(new Crumb[] {
        new Crumb("Dashboard", "", "fa-dashboard")
    });
}

<div class="row">
    <div class="col-md-6">
        @Html.Action("Widget", "Activities")
    </div>
    <div class="col-md-6">
        @Html.Action("Widget", "WorkItems")
        @Html.Action("Widget", "Goals")
    </div>
</div>