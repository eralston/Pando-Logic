@model PandoLogic.Models.Activity

@{
    if (Model.Type == PandoLogic.Models.ActivityType.Comment)
    {
        ViewBag.Title = "Delete Comment";
    }
    else
    {
        ViewBag.Title = "Delete Activity";
    }
}

<div class="row">
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">
                    <i class="fa @Model.IconClass circle"></i>
                    @ViewBag.Title
                </h3>

            </div>

            <div class="box-body">
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.User.FullName)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.User.FullName)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.CreatedDateUtc)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.CreatedDateUtc)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.Title)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Title)
                    </dd>

                    @if (!string.IsNullOrEmpty(Model.Description))
                    {
                        <dt>
                            @Html.DisplayNameFor(model => model.Description)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.Description)
                        </dd>
                    }
                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <button type="submit" class="btn btn-danger">@ViewBag.Title</button>
                        @Html.Partial("_Cancel", Model)
                    </div>
                }
            </div>
        </div>
    </div>
</div>
